<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.pragyan.dalal18">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/dalal_icon2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dalal_icon2_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="org.pragyan.dalal18.ui.LoginActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name="org.pragyan.dalal18.ui.MainActivity"
            android:parentActivityName="org.pragyan.dalal18.ui.LoginActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan" />

        <activity android:name="org.pragyan.dalal18.ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="org.pragyan.dalal18.ui.RegistrationActivity"
            android:parentActivityName="org.pragyan.dalal18.ui.LoginActivity"
            android:screenOrientation="portrait" />

        <service
            android:name="org.pragyan.dalal18.notifications.NotificationService"
            android:enabled="true" />

        <receiver
            android:name="org.pragyan.dalal18.notifications.NotificationBroadcastReceiver"
            android:enabled="true"
            android:exported="true"
            android:label="RestartServiceWhenStopped">

            <intent-filter>
                <action android:name="NotifServiceBroadcast" />
            </intent-filter>

        </receiver>

    </application>

</manifest>